/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/gbsw-ai-chatbot',
  images: {
    unoptimized: true,
  },
  reactStrictMode: true,
}

module.exports = nextConfig
